# Build Artifacts Directory

This directory is the fixed output root for one-click build scripts:

- `artifacts/windows-native/` for Windows-native build/package outputs
- `artifacts/linux-to-windows/` for Linux cross-compile to Windows outputs
- `artifacts/linux-native/` for Linux-native build outputs

Generated binaries and zip packages are not tracked by git by default.
